This is a very very simple view of GPT style blocks.
It uses torch modules.

Transformers have a complex version of torch based models.
This simple demo *does not* use transformers. Instead it shows how to build basic transformers building block.
